<?xml version="1.0" encoding="UTF-8"?>
<ContentView
    xmlns="http://xamarin.com/schemas/2014/forms"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:helpers="clr-namespace:WavesGraphs.Helpers"
    xmlns:controls="clr-namespace:WavesGraphs.Controls"
    x:Class="WavesGraphs.Controls.ModalHeader">

    <ContentView.Content>

        <FlexLayout
            Padding="10"
            AlignItems="Center"
            JustifyContent="SpaceBetween"
            Direction="Row">

            <controls:IconButton
                x:Name="close"
                TextColor="#494948"
                FontSize="Large"
                Text="{x:Static helpers:IconsHelper.Close}" />

            <Label
                x:Name="title"
                FontAttributes="Bold"
                TextColor="#494948"
                FontSize="Medium" />

            <controls:IconButton
                x:Name="action"
                TextColor="#494948"
                FontSize="Large"
                Text="{x:Static helpers:IconsHelper.Check}" />

        </FlexLayout>

    </ContentView.Content>

</ContentView>
